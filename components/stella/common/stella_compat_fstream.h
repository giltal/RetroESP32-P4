/*
 * stella_compat_fstream.h — Redirect to stella_compat_streams.h
 * (backward-compat for files patched to include this)
 */
#ifndef STELLA_COMPAT_FSTREAM_H
#define STELLA_COMPAT_FSTREAM_H
#include "stella_compat_streams.h"
#endif
